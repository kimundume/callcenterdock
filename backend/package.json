{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "node -e \"const fs=require('fs'); if(!fs.existsSync('dist/data')){fs.mkdirSync('dist/data', {recursive: true});} console.log('Data directory ensured');\"",
    "build": "tsc && npm run verify-build && npm run copy-data",
    "verify-build": "node -e \"const fs=require('fs'); const path=require('path'); const requiredFiles=['dist/data/persistentStorage.js','dist/sockets/signaling.js','dist/server.js']; requiredFiles.forEach(f=>{if(!fs.existsSync(f)){console.error('Missing required file:', f); process.exit(1);}else{console.log('✓', f);}}); console.log('Build verification complete!');\"",
    "copy-data": "node -e \"const fs=require('fs'); const path=require('path'); if(!fs.existsSync('dist/data')){fs.mkdirSync('dist/data', {recursive: true});} console.log('Data directory ensured');\"",
    "start": "node dist/server.js",
    "dev": "nodemon src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/nodemailer": "^6.4.17",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.0.0",
    "nodemailer": "^7.0.5",
    "socket.io": "^4.8.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/express": "^5.0.3",
    "@types/express-rate-limit": "^6.0.2",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.1.0",
    "@types/socket.io": "^3.0.2",
    "@types/uuid": "^10.0.0",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
