{
  "name": "calldocker",
  "version": "1.0.0",
  "description": "Multi-tenant browser-based voice calling platform",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend/dashboard && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend/dashboard && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/dashboard && npm install"
  },
  "keywords": [
    "webrtc",
    "voice-calling",
    "call-center",
    "widget"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.8.3"
  }
}
